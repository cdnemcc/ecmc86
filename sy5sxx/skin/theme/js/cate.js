$(function(){
	(function(){
        var buttonSelector = '.m-cate-nav .u-cate-nav-item';
        var wrapperSelector = '.m-checker-wrapper .m-checker-item';
        InitTool.checkListen(wrapperSelector,buttonSelector);
    })();
});